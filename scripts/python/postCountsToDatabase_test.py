import unittest

import PostCountsToDatabase

class Test_PostCountsToDatabase(unittest.TestCase):
    def test_main(self):
        args = {'count_file': os.path,
                'sample_name': ,
                'fastq_file_number': ,
                'counts_url': ,
                'qc_url': ,
                'post': True}
        PostCountsToDatabase.main([])